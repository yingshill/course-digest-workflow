# §8 · Open questions

1. ~~Should the Course Notebook template include per-module sub-pages or per-module toggle sections?~~ — ✅ **Resolved.** Toggle sections with AI-generated module content.
2. ~~Should there be a `Difficulty` property?~~ — ✅ **Resolved.** Added (Beginner / Intermediate / Advanced).
3. ~~Does `Transcript Ready` work as trigger name?~~ — ✅ **Resolved.** URL-first workflow. Agent scrapes URL on page creation. Manual paste as fallback for login-gated platforms. `Transcript Ready` property later removed — Status tracks completion instead.
4. **Pre-existing shared vocabulary drift** surfaced during audit:
   - Podcast Digest missing `Category` property entirely
   - AI Daily Hits missing 4 Category options + has "Reflective" instead of "Good to know" in Eval Metric
   - **Disposition:** Out of scope for this project — flagged for a separate System Audit session.
